package com.trippals.shared.platform

/**
 * Android app composition supplies an FCM-backed implementation only after the
 * Firebase project and google-services configuration are provisioned. This
 * boundary intentionally has no fabricated device token fallback.
 */
